# Copia este archivo como .env y configura las variables según tu entorno

# Puerto del servidor
PORT=3000

# Entorno de ejecución (development | production)
NODE_ENV=development

# CORS — orígenes permitidos separados por coma
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Swagger (true|false)
SWAGGER_ENABLED=true

# MongoDB — base local/deploy: AmesCotizadorDB
MONGODB_URI=mongodb://localhost:27017/AmesCotizadorDB

# JWT — en production NO usar placeholders inseguros
JWT_SECRET=cambiar-por-secreto-largo-en-local
JWT_EXPIRES_IN=8h

# Frontend (magic links, reset password)
FRONTEND_URL=http://localhost:5173

# Email (SMTP) — host/port/from de plataforma.
# EMAIL_USER/PASS globales: password-reset y fallback BCC plataforma (Story 3.3).
# Outbound cotización / notif interna: auth = emailUser + decrypt(emailSecretEnc)
# del tenant (TENANT_SECRETS_KEY); host/port siguen siendo estos.
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=

# Cifrado at-rest de app passwords por tenant (Story 3.2–3.3 / AD-12).
# 32 bytes en hex (64 chars). Generar:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NUNCA reutilizar un valor de ejemplo ni la misma clave entre entornos.
# Requerido para enviar cotizaciones con credenciales del tenant.
TENANT_SECRETS_KEY=

# Tenant por defecto hasta Story 1.5 (AD-2)
DEFAULT_TENANT_CLAVE=queretaro
